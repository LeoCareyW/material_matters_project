<div>
  <%= simple_form_for(@partner) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :photos, as: :file, input_html: { multiple: true } %>
    <%= f.submit "Submit" %>
    <% end %>
</div>
